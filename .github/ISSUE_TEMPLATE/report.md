---
name: A figure that does not match
about: The site shows one thing, the chain or the issuer shows another
title: "[ticker] what the plate says vs what the chain says"
labels: ""
---

**Ticker and page**
Which name, which page (`/`, `/events/{ticker}`, `/api/ticker/{ticker}`).

**What the site printed**
Paste the plate line or the API row, with the block and read time the page shows.

**What the chain or the issuer says**
The raw `eth_call` result, the explorer link, or the issuer feed entry, with the block you read it at.

**Anything else**
Cache headers you saw, time between the two readings, whether it repeats.
