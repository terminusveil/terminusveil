{
  "name": "terminus-veil-contracts",
  "private": true,
  "description": "Terminus Veil on-chain pieces: the Wire (phase 1) and the Pass (phase 2). Hardhat project; deploy with the owner's wallet.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:robinhood": "hardhat run scripts/deploy.js --network robinhood",
    "verify:robinhood": "hardhat verify --network robinhood",
    "deploy:page": "node deploy-page/server.mjs"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.6.1"
  }
}
